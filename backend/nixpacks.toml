[phases.setup]
nixPkgs = ["php83", "php83Packages.composer", "nginx", "nodejs"]

[phases.build]
cmds = [
    "composer install --no-dev --optimize-autoloader",
    "npm install && npm run build"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=8000"
